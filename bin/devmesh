#!/usr/bin/env sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
BUNDLE_DIR=$(CDPATH= cd -- "$SCRIPT_DIR/.." && pwd)
exec "$BUNDLE_DIR/runtime/bin/java" -jar "$BUNDLE_DIR/devmesh.jar" "$@"
